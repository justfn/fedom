/* ** 数组专用  
*/

import { VaryValue, } from "./Vary.js";

export default function VaryList(list){
  const varyedList = VaryValue(list);
  
  // insert(idx,val);
  // remove(idx)
  
  return varyedList;
} 
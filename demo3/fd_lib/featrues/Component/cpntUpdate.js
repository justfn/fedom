

export default function cpntUpdate(fNode, context, instance){
  fNode.context = context;
  fNode.instance = instance;
  // fNode.varyTag.kill();
  // fNode.context = {}; 
  // fNode.instance = {}; 
  
  
  
} 
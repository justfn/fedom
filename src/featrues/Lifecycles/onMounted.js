/* ** 组件渲染后回调 
*/


export default function onMounted(fdNode){
  // to_do_: setTimeout待优化  
  // setTimeout(()=>{
  //   if (fdNode.context._mountedFns && fdNode.context._mountedFns.length ) {
  //     fdNode.context._mountedFns.forEach((fn)=>{
  //       fn && fn(fdNode);
  //     });
  //   }
  //   if (fdNode.context.onMounted) {
  //     fdNode.context.onMounted(fdNode);
  //   }
  // 
  // })
} 



